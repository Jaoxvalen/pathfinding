#pragma once
#include "Drawer.h"
#include "CGraph.h"

using namespace visual;
using namespace DS;
using namespace std;

class Context
{
    Drawer mDrawer;
public:
    void draw()
    {
        mDrawer.line(100,100,200,200,new Color(255,0,0,255));
    }
    void loadGraph(){
        DS
    }
};